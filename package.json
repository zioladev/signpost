{
  "name": "signpost",
  "version": "0.1.0",
  "private": true,
  "description": "A stateless capability resolver for WebMCP.",
  "scripts": {
    "test": "node verify.mjs && node test-dashboard.mjs && node test-evidence.mjs",
    "test:browser": "node verify-browser.mjs"
  },
  "devDependencies": {
    "playwright": "^1.55.0"
  }
}
